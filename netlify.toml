[build]
  command = "cd v2 && npm install && npm run build"
  publish = "v2/dist"
  functions = "v2/netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
